{
  "name": "discord-companion-bot",
  "version": "1.0.0",
  "description": "Multi-companion Discord bot Soul Worker - MCP polling pattern",
  "main": "src/index.ts",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.11.0",
    "agents": "^0.0.98",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.100.0"
  }
}
